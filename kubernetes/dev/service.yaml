apiVersion: v1
kind: Service
metadata:
  name: bsoft-frontend-dev
  namespace: dev
spec:
  selector:
    app: bsoft-frontend
    environment: dev
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
      nodePort: 30081
  type: NodePort