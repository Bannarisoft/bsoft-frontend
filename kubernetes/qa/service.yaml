apiVersion: v1
kind: Service
metadata:
  name: bsoft-frontend-qa
  namespace: qa
spec:
  selector:
    app: bsoft-frontend
    environment: qa
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
      nodePort: 30082
  type: NodePort