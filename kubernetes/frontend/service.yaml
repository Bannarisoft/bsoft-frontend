apiVersion: v1
kind: Service
metadata:
  name: bsoft-frontend
spec:
  selector:
    app: bsoft-frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
      nodePort: 30080  # Optional: static NodePort (between 30000–32767)
  type: NodePort
