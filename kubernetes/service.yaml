apiVersion: v1
kind: Service
metadata:
  name: bsoft-frontend-service
spec:
  selector:
    app: bsoft-frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
  type: LoadBalancer
